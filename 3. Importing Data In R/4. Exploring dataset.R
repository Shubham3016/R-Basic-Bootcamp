# Exploring Data into R

# R have by default the datasets which can be display using following datset


datasets::cars

nrow(cars) # Gives the total rows of data
ncol(cars) # Gives the total column of data

head(cars)
tail(cars)


head(cars,n=10) # gives to total 10 rows 

tail(cars,n=10) # gives to total 10 rows  

str(cars)

summary(cars) # Gives the summary of whole data in statistical format 


